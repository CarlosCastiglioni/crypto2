import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  graph: {
    borderRadius: 10,
    marginLeft: 40,
    marginRight: 40,
    marginBottom: 5,
  },
});

export default styles;
